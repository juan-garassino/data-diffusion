[project]
name = "datadiffusion"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "torch",
  "numpy",
  "pandas",
  "matplotlib",
  "seaborn",
  "mlflow",
  "pytest",
  "scikit-learn",
  "scipy",
  "optuna",
  "pyyaml",
  "tqdm",]

[tool.uv]
package = false
